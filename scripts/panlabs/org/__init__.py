"""A configuração de org e de repositório que não é ruleset.

Mesma instância do seam `plan(observed, desired) -> Plan` / `apply(Plan)` do
`panlabs.ruleset`, sobre outra dimensão de observação: a política de Actions que
sustenta a esteira, as features de segurança, a vitrine e as features desligadas.
"""
